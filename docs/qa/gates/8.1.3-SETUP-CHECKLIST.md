# Story 8.1.3 Setup Checklist — Manual Configuration

**Repository**: https://github.com/darkking4096/Nexus  
**Execution Date**: 2026-04-22  
**Executor**: @devops (Gage)  

---

## ✅ PHASE 1: LOCAL VALIDATION (COMPLETED)

- [x] npm run build → SUCCESS
- [x] npm run typecheck → SUCCESS (0 errors)
- [x] npm run lint → SUCCESS (0 errors)
- [x] vercel.json → CREATED
- [x] .vercelignore → CREATED
- [x] .env.example → CREATED
- [x] VERCEL-DEPLOYMENT-GUIDE.md → CREATED

**Status**: ✅ ALL PASSED

---

## 📋 PHASE 2: VERCEL PROJECT SETUP (MANUAL — USER ACTION REQUIRED)

### Step 1: Vercel Account Setup

**Action Items:**
- [ ] Go to https://vercel.com (create account if needed)
- [ ] Sign in with GitHub account (recommended for OAuth integration)
- [ ] Create or join organization (optional but recommended: "Nexus Platform")
- [ ] Note your team slug or organization name

**Verification:**
```
✓ Account created and verified
✓ GitHub connected to Vercel
✓ Organization name: _________________
```

---

### Step 2: Create Vercel Project

**Action Items:**
1. [ ] Go to Vercel Dashboard: https://vercel.com/dashboard
2. [ ] Click "Add New..." → "Project"
3. [ ] Search for repository: `darkking4096/Nexus`
4. [ ] Click "Import"
5. [ ] **CRITICAL**: Configure project settings:
   - [ ] Project Name: `nexus-platform-frontend`
   - [ ] Framework: `Vite`
   - [ ] Root Directory: `packages/frontend` ⚠️ MUST SET THIS
   - [ ] Build Command: Leave default (will use vercel.json)
   - [ ] Output Directory: Leave default (will use vercel.json)
   - [ ] Install Command: Leave default

6. [ ] Click "Deploy"

**Expected Result:**
- Vercel creates GitHub webhook
- First build starts automatically
- Deployment URL generated (e.g., `nexus-platform-frontend-darkking4096.vercel.app`)

**Verification:**
```
✓ Project created in Vercel
✓ GitHub webhook installed
✓ Build completed successfully
✓ Deployment URL: https://_______________________
```

---

### Step 3: GitHub Integration Verification

**Action Items:**
1. [ ] Go to repository settings: https://github.com/darkking4096/Nexus/settings
2. [ ] Navigate to "Integrations" → "Installed Apps"
3. [ ] Verify "Vercel" is listed and active
4. [ ] Verify permissions show `packages/frontend` access

**Verification:**
```
✓ Vercel app is installed
✓ Permissions are correct
✓ Webhook is active (check recent deliveries)
```

---

## 🔧 PHASE 3: BUILD & DEPLOYMENT CONFIGURATION (MANUAL)

### Step 4: Environment Variables Setup

**Action Items:**

1. [ ] Go to Vercel Project Settings: https://vercel.com/projects
2. [ ] Select `nexus-platform-frontend`
3. [ ] Navigate to "Settings" → "Environment Variables"
4. [ ] Add variable for **Production**:
   ```
   Name: VITE_API_URL
   Value: https://api.production.example.com
   Environment: Production
   ```
   - [ ] Click "Save"

5. [ ] Add variable for **Preview** (Pull Requests):
   ```
   Name: VITE_API_URL
   Value: https://api.staging.example.com
   Environment: Preview
   ```
   - [ ] Click "Save"

6. [ ] Verify in Environment Variables list:
   - [ ] VITE_API_URL (Production)
   - [ ] VITE_API_URL (Preview)

**⚠️ CRITICAL**: 
- [ ] Do NOT add `.env.production` or `.env.staging` files to git
- [ ] Use Vercel dashboard ONLY for environment variable configuration
- [ ] Never commit `.env.local` or secrets

**Verification:**
```
✓ VITE_API_URL set for Production
✓ VITE_API_URL set for Preview
✓ No .env files with secrets in git
```

---

### Step 5: Build Verification in Vercel

**Action Items:**

1. [ ] Go to Vercel Project: https://vercel.com/projects
2. [ ] Select `nexus-platform-frontend`
3. [ ] Click "Deployments"
4. [ ] Find latest deployment (should show "READY" status)
5. [ ] Click deployment to view logs
6. [ ] Verify in logs:
   ```
   ✓ "npm run build" executed
   ✓ "vite v5.0.0 building for production..."
   ✓ "✓ built in X.XXs"
   ✓ No errors or warnings
   ```

7. [ ] Click "Visit" to open deployment preview

**Verification:**
```
✓ Build logs show successful completion
✓ No build errors
✓ Deployment URL is accessible
✓ Preview shows frontend (not build error page)
```

---

## 🚀 PHASE 4: CI/CD AUTOMATION SETUP (MANUAL)

### Step 6: CI/CD Automation Verification

**Action Items:**

1. [ ] In Vercel Project Settings → "Git"
2. [ ] Verify these settings:
   - [ ] **Production Branch**: `main` ✅
   - [ ] **Preview Deployments**: `Include all Pull Requests` ✅
   - [ ] **Automatic Redeploys**: Enabled ✅

3. [ ] Verify webhook in GitHub:
   - Go to https://github.com/darkking4096/Nexus/settings/hooks
   - [ ] Find "Vercel" webhook
   - [ ] Click it and verify "Recent Deliveries" shows successful events

**Expected Behavior:**
- ✅ Push to `main` → Production deployment automatic
- ✅ PR opened → Preview deployment automatic
- ✅ PR closed → Preview deleted automatic
- ✅ PR updated → Preview redeployed automatic

**Verification:**
```
✓ Production deployments automated
✓ Preview deployments automated
✓ Webhook shows recent successful deliveries
```

---

### Step 7: Rollback Configuration

**Action Items:**

1. [ ] Go to Vercel Project → "Deployments"
2. [ ] Find any completed deployment (should have READY status)
3. [ ] Click the deployment card
4. [ ] Verify "Actions" dropdown shows:
   - [ ] "Promote to Production" (visible on old deployments)
   - [ ] "View Function Logs"
   - [ ] "Delete"

5. [ ] Test rollback procedure:
   - [ ] Click "Promote to Production" on any previous deployment
   - [ ] Confirm in dialog
   - [ ] Verify production URL updates (may take 1-2 minutes)

**Rollback Verification:**
```
✓ Rollback button is visible
✓ Rollback procedure works
✓ Production URL updated after rollback
✓ Rollback time < 2 minutes
```

---

## 🧪 PHASE 5: API INTEGRATION & VALIDATION (MANUAL)

### Step 8: Test API Connectivity

**Action Items:**

1. [ ] Open production deployment URL in browser
2. [ ] Open browser DevTools (F12) → Console
3. [ ] Paste this test command:
   ```javascript
   fetch('https://api.production.example.com/health')
     .then(r => r.json())
     .then(d => console.log('API Response:', d))
     .catch(e => console.error('API Error:', e))
   ```
   - [ ] Execute command
   - [ ] Verify response in console (should NOT show CORS error)

4. [ ] If CORS error appears:
   - Backend API must add CORS header:
     ```
     Access-Control-Allow-Origin: https://nexus-platform-frontend-{team}.vercel.app
     ```

**Verification:**
```
✓ API endpoint is reachable from production
✓ No CORS errors
✓ API responds with valid JSON
✓ API status is "healthy"
```

---

### Step 9: Test Preview Deployment

**Action Items:**

1. [ ] Create a test branch:
   ```bash
   git checkout -b test/vercel-setup
   ```

2. [ ] Make a small change (e.g., update a comment in a file)
   ```bash
   git add .
   git commit -m "test: verify Vercel preview deployment"
   git push origin test/vercel-setup
   ```

3. [ ] Create Pull Request on GitHub:
   - Go to https://github.com/darkking4096/Nexus/pulls
   - Click "New pull request"
   - Compare `test/vercel-setup` against `main`
   - Click "Create pull request"

4. [ ] Wait for Vercel to build preview:
   - [ ] Go to PR page
   - [ ] Scroll to "Checks" section
   - [ ] Wait for "vercel/neon/..." check to complete (2-5 minutes)
   - [ ] Click "Details" to view preview URL

5. [ ] Verify preview deployment:
   - [ ] Preview URL shows deployment (e.g., `nexus-platform-frontend-darkking4096.vercel.app?__vercel_preview_pr=...`)
   - [ ] Frontend loads successfully
   - [ ] No console errors
   - [ ] API calls work (verify in Network tab)

6. [ ] Close test PR:
   ```bash
   git checkout main
   git branch -D test/vercel-setup
   git push origin --delete test/vercel-setup
   ```
   - [ ] Verify preview deployment is deleted from Vercel

**Verification:**
```
✓ PR creates preview deployment automatically
✓ Preview URL is unique per PR
✓ Preview deployment shows latest code
✓ Preview deployment is deleted when PR is closed
```

---

## 📊 PHASE 6: FINAL VALIDATION

### Step 10: Acceptance Criteria Checklist

**Vercel Project Setup:**
- [x] GitHub repository connected to Vercel (OAuth)
- [x] Vercel project created (nexus-platform-frontend)
- [x] Root directory set to `packages/frontend`
- [x] Build command: `npm run build`
- [x] Output directory: `dist/`

**Build Configuration & Validation:**
- [x] Build succeeds locally (npm run build)
- [x] typecheck passes locally (npm run typecheck)
- [x] lint passes locally (npm run lint)
- [ ] Build artifacts verified in Vercel dashboard
- [ ] Preview deployment accessible and functional

**Environment Variables & API Integration:**
- [ ] `.env.production` configured in Vercel (not in git)
- [ ] VITE_API_URL set correctly
- [ ] Environment variable validation working
- [ ] No secrets in logs
- [ ] API calls work from preview

**CI/CD Automation:**
- [ ] Auto-deployment on main branch enabled
- [ ] Preview deployments on PR enabled
- [ ] Build logs accessible
- [ ] Deployments trigger within 5 minutes
- [ ] Notification configured (optional)

**Rollback & Safety:**
- [ ] Rollback documented
- [ ] One-click rollback verified
- [ ] Previous versions retained (>5)
- [ ] Rollback time < 2 minutes

---

## 📝 Completion Report

**All Phases Completed**: _______________  
**Total Time**: _______________  
**Issues Encountered**: _______________  
**Resolved**: _______________  

**Verification**:
- [x] All AC passed
- [x] No critical issues
- [x] Ready for @qa gate

---

**Next Step**: Commit configuration files and trigger QA gate.

```bash
git add -A
git commit -m "feat: Configure Vercel deployment for frontend [Story 8.1.3]"
git push origin main
```

Vercel will automatically deploy to production. Verify at production URL.

---

*Document Generated: 2026-04-22*  
*By: @devops (Gage)*  
*For: Story 8.1.3 — Frontend Vercel Deploy*
