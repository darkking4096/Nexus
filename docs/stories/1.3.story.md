# Story 1.3: Playwright MCP Integration & Publishing Test

**Epic**: 1 - Foundation & Setup  
**Status**: Ready  
**Priority**: MUST (P0)  
**Date Created**: 2026-04-07  
**Depends On**: Story 1.1, 1.2  
**Owner**: @dev (Dex)  

---

## 📋 Descripción

Integrar **Playwright MCP** (browser automation) para publicar posts no Instagram sem ser detectado como bot. Criar teste funcional de publicação em conta de teste.

**Motivação**: Instagrapi é OK para ler dados, mas para publicação segura (zero bot detection), usamos Playwright que simula usuário real com delays aleatórios e comportamento humano.

---

## 🎯 Critérios de Aceitação

- [ ] Playwright MCP configurado em `.claude/mcp.json`
- [ ] Service `PublishService` implementado para feed posts
- [ ] Publicação de imagem + caption no Instagram
- [ ] Delays aleatórios implementados (1-5s entre ações)
- [ ] Tratamento de erros (login failed, rate limit, network)
- [ ] Teste E2E em conta de teste (publicar 1 post)
- [ ] Logs de publicação (timestamp, status, errors)
- [ ] Retry logic (max 3 tentativas, backoff exponencial)
- [ ] Testes unitários para PublishService
- [ ] Documentação de como configurar Playwright
- [ ] Sem erros de linting

---

## 📐 Escopo Técnico

### 1. Playwright MCP Setup

**O que fazer**:
- [ ] Verificar que Playwright MCP está disponível em Claude Code
- [ ] Criar `backend/src/services/PlaywrightService.ts`
- [ ] Implementar interface básica:
  ```typescript
  class PlaywrightService {
    async launchBrowser(): Promise<Browser>
    async closeBrowser(): Promise<void>
    async login(username: string, password: string): Promise<void>
    async navigateToFeed(): Promise<void>
    async publishPost(imageUrl: string, caption: string, hashtags: string[]): Promise<PostResult>
  }
  ```

**Decisão de Design Esperada**:
- Usar pool de browsers para publicações paralelas?
- Como gerenciar sessões (login uma vez vs. a cada publicação)?

### 2. Publishing Flow

**POST /api/content/publish**
```json
Request: {
  "profile_id": "...",
  "image_url": "https://...",
  "caption": "...",
  "hashtags": ["tag1", "tag2"]
}
Response: {
  "post_id": "...",
  "instagram_url": "https://instagram.com/p/...",
  "status": "published",
  "published_at": "2026-04-07T10:30:00Z"
}
```

**O que fazer**:
- [ ] Criar `backend/src/services/PublishService.ts`
- [ ] Implementar `publishPost(profileId, content)` que:
  1. Recupera credenciais do profile (desencripta)
  2. Inicia browser via Playwright
  3. Faz login no Instagram
  4. Navega para feed
  5. Clica em "Nova publicação"
  6. Faz upload de imagem
  7. Adiciona caption e hashtags
  8. Publica
  9. Registra post_id em database
  10. Fecha browser
- [ ] Adicionar rota em `backend/src/routes/content.ts`

### 3. Behavioral Realism (Anti-Bot Detection)

**Implementar**:
- [ ] Random delays entre ações (1-5 segundos)
- [ ] Mouse movements (não apenas cliques)
- [ ] Scroll behavior realista
- [ ] Viewport size variável (não sempre 1920x1080)
- [ ] User-Agent realista (Chrome on Windows, etc)

```typescript
async function randomDelay(min = 1000, max = 5000): Promise<void> {
  const delay = Math.random() * (max - min) + min;
  await new Promise(resolve => setTimeout(resolve, delay));
}

// Usar antes de cada ação:
await page.mouse.move(x, y);
await randomDelay(1, 3);
await page.click(selector);
```

### 4. Error Handling & Retry Logic

**Cenários**:
- [ ] Login failed (invalid creds, account locked)
- [ ] Rate limited (aguardar 1h ou mais)
- [ ] Network timeout (retry com backoff: 2s, 4s, 8s)
- [ ] Element not found (página mudou, retry)
- [ ] Image upload failed (retry com imagem diferente)

```typescript
async function retryWithBackoff<T>(
  fn: () => Promise<T>,
  maxAttempts = 3,
  initialDelay = 2000
): Promise<T> {
  for (let i = 0; i < maxAttempts; i++) {
    try {
      return await fn();
    } catch (error) {
      if (i === maxAttempts - 1) throw error;
      const delay = initialDelay * Math.pow(2, i);
      await new Promise(resolve => setTimeout(resolve, delay));
    }
  }
  throw new Error('Max retries exceeded');
}
```

### 5. Database Changes

**Adicionar a content table**:
```sql
ALTER TABLE content ADD COLUMN (
  instagram_post_id TEXT,
  instagram_url TEXT,
  published_by TEXT,
  publish_error TEXT,
  retry_count INTEGER DEFAULT 0
);
```

**Nova tabela para logs**:
```sql
CREATE TABLE publish_logs (
  id TEXT PRIMARY KEY,
  profile_id TEXT NOT NULL REFERENCES profiles(id),
  content_id TEXT REFERENCES content(id),
  action TEXT,
  status TEXT,
  error_message TEXT,
  timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 6. Testes

**Testes Unitários**:
- [ ] PublishService: delay functions, retry logic
- [ ] Error handling: invalid credentials, timeouts

**Testes E2E**:
- [ ] Publicar 1 post em conta de teste
- [ ] Verificar se post aparece no feed
- [ ] Validar caption e hashtags
- [ ] Testar retry logic (simular failure)

**Test Account Setup**:
- [ ] Criar ou usar conta Instagram de teste específica
- [ ] Documentar credenciais (de forma segura)
- [ ] Estabelecer que posts de teste podem ser deletados

---

## ⚠️ Considerações Especiais

1. **Instagram Anti-Bot**:
   - Instagram detecta automação
   - Delays randômicos + mouse movements ajudam
   - Considerar usar proxies em produção (future enhancement)

2. **Browser Lifecycle**:
   - Abrir browser = 3-5 segundos
   - Considerar manter browser aberto para múltiplas publicações?
   - Implementar timeout (fechar após 30 min inativo)

3. **Credenciais Instagram**:
   - Armazenar com AES-256-GCM
   - NUNCA logar com conta pessoal do dev
   - Usar test account ou conta cliente específica

4. **Rate Limiting**:
   - Instagram limita publicações a ~5-10 por hora por conta
   - Implementar fila de publicação com scheduling
   - Respeitar horários agendados

---

## 📦 Dependências Externas

| Dependência | Status |
|------------|--------|
| Playwright MCP | ✅ Configurado em Claude Code |
| Instagram Account (Business, test) | ⚠️ Preparar |
| chromium/firefox | ✅ Playwright instala |

---

## 🔄 Workflow

**Fase 1: Playwright Setup** (@dev)
- Configurar MCP
- Criar PublishService skeleton

**Fase 2: Publishing Logic** (@dev)
- Implementar fluxo de publicação
- Anti-bot measures

**Fase 3: Error Handling & Retry** (@dev)
- Tratamento de erros
- Retry logic

**Fase 4: Testes E2E** (@qa)
- Testar em conta real
- Validar publication

**Fase 5: Validation** (@po)
- Aceitar story

---

## 📂 File List

- [ ] `backend/src/services/PlaywrightService.ts` — Playwright wrapper
- [ ] `backend/src/services/PublishService.ts` — Publishing logic
- [ ] `backend/src/routes/content.ts` — Publish routes
- [ ] `backend/src/utils/retry.ts` — Retry logic
- [ ] `backend/tests/PublishService.test.ts` — Tests
- [ ] `docs/playwright-setup.md` — Documentation

---

## 🚀 Status Tracking

**Progress**: 0%

- **Phase 1 (Setup)**: [ ] Pending
- **Phase 2 (Logic)**: [ ] Pending
- **Phase 3 (Error Handling)**: [ ] Pending
- **Phase 4 (Tests)**: [ ] Pending
- **Phase 5 (Validation)**: [ ] Pending

---

**Próxima Story**: 1.4 - Marketing Instagram Squad Integration (Research Module)
